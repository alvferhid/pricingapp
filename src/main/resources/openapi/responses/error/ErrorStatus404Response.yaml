type: object
properties:
  error:
    type: object
    properties:
      code:
        type: integer
        example: 404
      message:
        type: string
        example: "Not Found"
      details:
        type: string
        example: "The requested resource was not found."
